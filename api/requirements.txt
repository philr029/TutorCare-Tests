# Combined requirements for Vercel deployment.
# Vercel's Python runtime uses this file (api/requirements.txt takes priority
# over root requirements.txt for functions built from api/main.py).

# --- Core library dependencies ---
requests>=2.31.0
dnspython>=2.4.0
phonenumbers>=8.13.0
cryptography>=41.0.0
click>=8.1.0
pyyaml>=6.0.1
urllib3>=2.1.0
beautifulsoup4>=4.12.0

# --- FastAPI / ASGI runtime ---
fastapi>=0.115.5
uvicorn[standard]>=0.32.1
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
python-multipart>=0.0.22
httpx>=0.28.0

# --- Rate limiting (in-memory; Redis optional) ---
slowapi>=0.1.9
